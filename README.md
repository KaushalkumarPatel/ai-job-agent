# Robotics Job Agent 🤖

AI Agent that scrapes robotics job listings from multiple job portals.

## Setup
pip install -r requirements.txt

## Usage
python main.py --llm kimi --prompt "Find robotics jobs in Germany" --websites linkedin indeed

## Project Structure
(Will be updated as project develops)